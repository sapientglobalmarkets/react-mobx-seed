import React from 'react';
import {observer} from 'mobx-react';
import style from './{{dashCase name}}.css';

export default observer(['store'], function {{ properCase name }}(props) {

    return (
        <div className={style.root}>
            <h2>{{ properCase name }}</h2>
        </div>
    );

});

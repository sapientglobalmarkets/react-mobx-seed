import React from 'react';
import {shallow} from 'enzyme';
import { {{properCase name}} } from './';

describe('<{{properCase name}} />', ()=> {

    it('should render correctly', ()=> {
        const dom = shallow(<{{properCase name}} />);
        expect(dom).to.have.length(1);
    });

});
